You are working in an existing Spring Boot Maven project for a Credit Decision Stand-in Service.

Implement the **Drools runtime execution pipeline for the knockout stage** using the DRL already generated by the authoring pipeline.

Context:
- The project already has a knockout authoring pipeline that extracts Excel rows, sends compact content to Tachyon, validates the returned artifact, and produces `renderedDrl`.
- Now implement the runtime side that can execute this DRL against an application request payload.
- This is only for knockout-stage execution for now.

==================================================
HIGH-LEVEL GOAL
==================================================

Build a runtime pipeline:

generated DRL -> compile with Drools -> execute against ExecutionContext -> create knockout policies in decision details -> return enriched result

Do not implement the full credit decision engine yet.
Do not implement all stages yet.
Only implement the Drools runtime integration needed to execute knockout DRL.

==================================================
BASE PACKAGE
==================================================

Use this exact base package:

com.example.creditdecision

==================================================
FILES TO ADD
==================================================

Create these files exactly:

src/main/java/com/example/creditdecision/drools/model/ExecutionContext.java
src/main/java/com/example/creditdecision/drools/service/PolicyCreationService.java
src/main/java/com/example/creditdecision/drools/service/DroolsCompilationService.java
src/main/java/com/example/creditdecision/drools/service/DroolsExecutionService.java
src/main/java/com/example/creditdecision/drools/service/KnockoutDroolsRuntimeService.java
src/main/java/com/example/creditdecision/api/KnockoutDroolsRuntimeController.java

==================================================
FILES TO UPDATE
==================================================

Update existing model classes only if needed so the DRL can compile and run:
- Application / request model
- Applicant model
- Bureau model
- DecisionDetails model
- Policy model
- BusinessTermSet model if it exists

Do not refactor unrelated code.

==================================================
1) CREATE ExecutionContext.java
==================================================

Package:
com.example.creditdecision.drools.model

Use Lombok @Data.

Fields:
- Application application
- Applicant primaryApplicant
- BusinessTermSet businessTermSet
- DecisionDetails decisionDetails

Behavior:
- decisionDetails should be initialized if null
- if decisionDetails.policies is null, initialize empty list
- provide getters used by DRL:
  - getApplication()
  - getPrimaryApplicant()
  - getBusinessTermSet()
  - getDecisionDetails()

This class is the single fact inserted into Drools.

==================================================
2) CREATE PolicyCreationService.java
==================================================

Package:
com.example.creditdecision.drools.service

Implement as a utility class with static method:

public static void addPolicy(
    ExecutionContext ctx,
    String policyCode,
    String policyCategory,
    String policyDescription
)

Behavior:
- ensure ctx.decisionDetails exists
- ensure ctx.decisionDetails.policies exists
- create Policy object
- set:
  - policyCode
  - policyCategory
  - policyDescription
- append to policies list
- do simple dedupe by policyCode so same rule does not insert duplicates

Also add overloaded method if useful:

public static void addPolicy(
    ExecutionContext ctx,
    String ruleId,
    String policyCode,
    String policyCategory,
    String policyDescription
)

If Policy has a ruleId/source field, populate it. Otherwise ignore ruleId safely.

==================================================
3) CREATE DroolsCompilationService.java
==================================================

Package:
com.example.creditdecision.drools.service

Use @Service.

Implement method:
public KieBase compileDrl(String drl)

Behavior:
- compile DRL from in-memory string using KieServices
- use KieFileSystem
- write DRL to virtual path such as src/main/resources/rules/knockout/generated-knockout.drl
- build with KieBuilder
- if there are any Message.Level.ERROR results, throw IllegalStateException with full compile errors
- return KieBase

==================================================
4) CREATE DroolsExecutionService.java
==================================================

Package:
com.example.creditdecision.drools.service

Use @Service.
Use @RequiredArgsConstructor.

Dependency:
- DroolsCompilationService

Implement method:
public ExecutionContext execute(String drl, ExecutionContext context)

Behavior:
- compile DRL into KieBase
- create KieSession
- insert context
- fireAllRules
- dispose session
- return updated context

==================================================
5) CREATE KnockoutDroolsRuntimeService.java
==================================================

Package:
com.example.creditdecision.drools.service

Use @Service.
Use @RequiredArgsConstructor.

Dependency:
- DroolsExecutionService

Implement method:
public ExecutionContext run(String drl, Application application)

Behavior:
- resolve primary applicant from application.applicant where primaryInd = 1
- build BusinessTermSet if your project already has one, otherwise create empty/default BusinessTermSet
- ensure DecisionDetails exists on application
- build ExecutionContext
- call droolsExecutionService.execute(drl, context)
- copy updated decisionDetails back onto application if needed
- return context

For MVP:
- primary applicant only
- if no primary applicant found, throw IllegalStateException with clear message

==================================================
6) CREATE KnockoutDroolsRuntimeController.java
==================================================

Package:
com.example.creditdecision.api

Use @RestController
Use @RequestMapping("/runtime/knockout")
Use @RequiredArgsConstructor

Dependency:
- KnockoutDroolsRuntimeService

Add POST endpoint:
@PostMapping("/execute")

Request DTO fields:
- String drl
- Application application

Response:
- return the ExecutionContext after rule execution

Use Lombok @Data for nested request DTO.

==================================================
7) IMPORTANT DRL RENDERING ADJUSTMENT
==================================================

If the current rendered DRL creates Policy objects inline, update the DRL renderer so generated rules call:

PolicyCreationService.addPolicy($ctx, "<policyCode>", "<policyCategory>", "<ruleName>");

or if ruleId is available:

PolicyCreationService.addPolicy($ctx, "<ruleId>", "<policyCode>", "<policyCategory>", "<ruleName>");

This is preferred over constructing Policy directly in each rule.

Also ensure the rendered DRL includes:
import com.example.creditdecision.drools.model.ExecutionContext;
import com.example.creditdecision.drools.service.PolicyCreationService;
import com.example.creditdecision.model.Policy;

If Policy import is no longer needed after helper-based creation, remove it.

==================================================
8) DROOLS DEPENDENCIES
==================================================

Add only if needed:
- drools-core
- drools-compiler
- kie-api
- kie-internal

Use versions compatible with the current project.

==================================================
9) ACCEPTANCE CRITERIA
==================================================

After implementation, the following should work:

1. I can take `renderedDrl` produced by the knockout authoring pipeline.
2. I can POST it along with an application payload to /runtime/knockout/execute.
3. The DRL compiles successfully.
4. Rules fire successfully.
5. Triggered policies are added to decisionDetails.policies.
6. Q18 and D22 both work against the primary applicant.
7. Duplicate policy inserts are prevented.

==================================================
10) AFTER IMPLEMENTATION
==================================================

After coding:
1. ensure project compiles
2. list files created/updated
3. mention assumptions
4. provide sample request body for /runtime/knockout/execute
5. stop there

Now implement exactly this runtime Drools integration.
